There is no code in this lecture. We will do the following to introduce the key components of machine learning. We will also build a neural network using Excel, solving the same linear regression problem.
